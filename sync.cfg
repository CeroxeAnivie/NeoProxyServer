# sync.cfg
# =======================================================
# NeoKeyManager 中心服务器地址 (不填则仅使用本地模式)
MANAGER_URL=http://127.0.0.1:8080

# 验证 Token
MANAGER_TOKEN=nkm-node-token

# 当前节点唯一 ID (用于区分不同物理机，获取特异化端口)
NODE_ID=node-suqian
# =======================================================
